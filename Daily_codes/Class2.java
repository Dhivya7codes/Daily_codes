package apnacollege;

public class Class2 {
    public static void main(String[] args) {
        String name = "aman"; // length() function returns no of characters in the string
        /* strng is a reference variable beacuse it does not store the valure directly it stores the rerence
  (memory address)
   when do we use the keyword NEW : WHEN WE WANT TO CREATE A REFERENCE VARIABLE OR NO PRIMITIVE TYPE VARIABLE
   OPTIONAL IN STRING */
        System.out.println(name.length());
        //System.out.println(g);
        String name2 = "akku"; // concatenate function
        System.out.println(name+" "+"and"+" "+name2);
        System.out.println(name.charAt(1));
    }
}
